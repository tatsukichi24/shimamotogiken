# Visual Design System (DESIGN.md)

本ファイルは、AIエージェントがUIコード（HTML, CSS, React, など）を生成する際に参照するデザイン仕様書です。UIを構築する際は、必ずこのルールに従ってください。
（参考モデル: 「AIスパルタン」トップページのビジュアル）

## 1. トーン＆マナー
* **ブランドイメージ:** 力強い、硬派、権威的（戦士・鎧のビジュアルモチーフ）
* **コアバリュー:** 「本気で会社を変える」という熱量と信頼感を両立させる。派手すぎず、重厚感のあるプレミアム感を演出する

## 2. カラーパレット (Colors)
指定された役割（トークン）に応じて、以下のカラーコードを厳密に使用してください。
* **Primary (ブランドカラー・ゴールド):** `#C9A227` (例: 高級感のあるゴールド。ボタン、チェックアイコン、装飾ラインに使用)
* **Secondary (アクセント・レッド):** `#E5342A` (例: 情熱的なレッド。ロゴの一部、強調テキスト、バッジに使用)
* **Tertiary (アクセント・オレンジ):** `#E8792F` (例: CTAボタン専用のオレンジ。「無料診断」など行動喚起に使用)
* **Background Dark (メイン背景):** `#0E1522` 〜 `#1B2A3D` (例: ネイビー〜チャコールのグラデーション。ヒーローセクション全体の背景)
* **Background Light (ヘッダー背景):** `#FFFFFF` (例: グローバルナビゲーション部分のみ使用)
* **Text on Dark (暗背景上の本文):** `#FFFFFF` (例: ヒーローセクションの見出し・本文)
* **Text on Light (明背景上の本文):** `#1A1A1A` (例: ナビゲーションのメニュー文字)
* **Text Muted (薄いテキスト):** `#9CA3AF` (例: 補足キャプション)
* **Border/Divider (境界線):** `rgba(255,255,255,0.15)` (例: 暗背景上の区切り線)

## 3. タイポグラフィ (Typography)
* **フォントファミリー (日本語):** `"Noto Sans JP", "Hiragino Kaku Gothic ProN", "Yu Gothic", sans-serif`
* **フォントファミリー (英数字):** `"Inter", system-ui, -apple-system, sans-serif`
* **ベースフォントサイズ:** `16px` (1rem)
* **見出し 1 (h1 / メインキャッチコピー):** `3rem〜4.5rem`, `line-height: 1.2`, 太字（Bold〜Black）。「AIスパルタン」のようなロゴタイプ的見出しは特大サイズで配置
* **見出し 2 (h2 / サブキャッチ):** `1.75rem`, `line-height: 1.4`, 太字。ボタン型の帯の中に配置することが多い
* **本文 (p):** `1rem〜1.125rem`, `line-height: 1.6`
* **強調ワード:** 文中の一部単語だけサイズアップ＋色変更（ゴールドまたはレッド）して視線を誘導する手法を多用する

## 4. スペースとレイアウト (Spacing)
余白や配置は、以下の8pxグリッドシステム（ベースは4px）を基準に設定してください。
* `spacing-1` (最小の余白): `8px`
* `spacing-2` (標準的な余白): `16px`
* `spacing-3` (セクション間の余白): `32px`
* `spacing-4` (大きな余白): `64px`
* `spacing-5` (ヒーローセクションの上下余白): `96px〜120px`
* **レイアウト方式:** CSS Flexbox / Grid を優先。ヒーローは「左：テキスト＋CTA」「右：キービジュアル（キャラクター等）」の2カラム構成、右端に縦積みのフローティングCTAバッジを重ねる構成を基本とする

## 5. コンポーネントスタイル (Components)
* **グローバルナビゲーション:**
  * 背景: 白 `#FFFFFF`
  * 高さ: `80px〜96px`
  * ロゴ左寄せ、メニュー右寄せ、フォントはやや太め・字間を広めに
* **プライマリボタン（帯型ボタン）:**
  * 背景: ゴールドのグラデーション（`#D4AF37` → `#B8860B`）
  * 角丸 (border-radius): `8px`
  * パディング: 縦 `16px`, 横 `32px`
  * シャドウ: `0 4px 12px rgba(0,0,0,0.25)`
  * 中の一部テキストを白背景バッジ（赤文字）で強調することがある
* **フローティングCTAバッジ（画面右端固定風）:**
  * 形状: 角丸の縦長カード、右端が画面外に切れるように配置
  * ゴールド系（情報系）とオレンジ系（相談・診断系）の2色を使い分け
  * 角丸: `12px`、内部に矢印アイコン「›」を添える
* **チェックリスト項目:**
  * 丸型ゴールドアイコン＋チェックマーク（背景円 `#C9A227`、アイコン白）
  * テキストは白・太字、項目下に薄い下線（`rgba(255,255,255,0.3)`）
  * 2カラムのグリッドで配置
* **動画/YouTube紹介カード:**
  * ゴールドの太枠（`2px solid #C9A227`）で縁取り
  * 左上に「CHECK!」の赤いリボン風バッジを斜めに配置
  * 内部にYouTubeロゴ＋番組タイトル＋出演者写真

## 6. レスポンシブ振る舞い (Responsive)
* **モバイルファースト**で設計すること（ただし本モデルはデスクトップ幅の大型ヒーローが主役のため、モバイルでは1カラムに再構成し、フローティングCTAバッジは画面下部固定バーに変換する）
* **ブレイクポイント:**
  * スマホ (Mobile): `~ 639px`
  * タブレット (Tablet): `640px ~ 1023px`
  * デスクトップ (Desktop): `1024px ~`

## 7. アクセシビリティ (Accessibility)
* 暗い背景上の白文字は十分なコントラストを確保し、ゴールド/オレンジ文字を背景に使う場合はコントラスト比 `4.5:1` 以上を確認すること
* 装飾的なキービジュアル画像には `alt=""`、意味を持つ画像には内容を説明する `alt` を必ず記述すること
* ボタン内の強調バッジ（赤文字など）は単独で読んでも意味が通じるようにすること

## 8. エージェントプロンプトガイド (Agent Prompt)
AIエージェントへの指示です。コードを生成する際は、以下のルールを遵守してください。
1. 上記のトークン（カラーコード・サイズ）をハードコードせず、可能な限りCSS変数として定義して使用してください。
2. ゴールド・レッド・オレンジの3色を同時に使いすぎないこと。1セクションにつき主役は1色（ゴールド基調＋差し色1つ）とする。
3. キービジュアルは実写/イラストの重厚な人物・シンボル画像を前提としたレイアウトにすること（画像は仮のプレースホルダーで良い）。
4. 日本語が不自然にならないよう、文字間隔（`letter-spacing`）や行間を適切に調整してください。
5. 未知のデザイン要素を生成する場合は、事前にユーザーに質問するか、このデザインシステムに準拠した形で提案してください。
